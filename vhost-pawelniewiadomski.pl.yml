---
  - hosts: proxy

    vars:
      name: pawelniewiadomski.pl
      listen: "*:80"
      server_name: pawelniewiadomski.pl
      redirect_url: http://pawelniewiadomski.pl.s3-website-us-east-1.amazonaws.com
      
    tasks:
      - include: tasks/setup-vhost-redirect.yml

    handlers:
      - include: handlers/handlers.yml
