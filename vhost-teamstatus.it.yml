---
  - hosts: proxy

    vars:
      name: teamstatus.it
      listen: "*:80"
      server_name: teamstatus.it
      redirect_url: http://teamstatus.it.s3-website-us-west-2.amazonaws.com

    tasks:
      - include: tasks/setup-vhost-redirect.yml

    handlers:
      - include: handlers/handlers.yml
