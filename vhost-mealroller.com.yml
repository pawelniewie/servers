---
  - hosts: proxy

    vars:
      name: mealroller.com
      listen: "*:80"
      server_name: mealroller.com
      redirect_url: http://mealroller.com.s3-website-us-west-2.amazonaws.com

    tasks:
      - include: tasks/setup-vhost-redirect.yml

    handlers:
      - include: handlers/handlers.yml
