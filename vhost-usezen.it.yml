---
  - hosts: proxy

    vars:
      name: usezen.it
      listen: "*:80"
      server_name: usezen.it
      redirect_url: http://usezen.it.s3-website-us-west-2.amazonaws.com

    tasks:
      - include: tasks/setup-vhost-redirect.yml

    handlers:
      - include: handlers/handlers.yml
