---
  - hosts: proxy

    vars:
      name: teamstatus.tv
      service: teamstatus
      service_domain: teamstatus.tv
      service_ip: 107.170.84.49
      service_port: 80

    tasks:
      - include: tasks/setup-proxy.yml

    handlers:
      - include: handlers/handlers.yml
