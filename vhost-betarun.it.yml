---
  - hosts: proxy

    vars:
      name: betarun.it
      listen: "*:80"
      server_name: betarun.it
      redirect_url: http://betarun.it.s3-website-us-west-2.amazonaws.com

    tasks:
      - include: tasks/setup-vhost-redirect.yml

    handlers:
      - include: handlers/handlers.yml
